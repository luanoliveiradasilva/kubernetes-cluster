apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels: 
    app: nginx
  namespace: aulainfra
spec:
  containers:
    - image: nginx
      name: nginx
      ports:
        - containerPort: 80
          name: nginx